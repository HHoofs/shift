[tox]
envlist = 
    py,
    cov
    type


[testenv]
deps = 
    pytest 
    coverage
commands =
    coverage run -m pytest --strict-markers tests/
    coverage report
    coverage html -d .tox/output/coverage/


[testenv:type]
deps = mypy
commands =
    mypy shift
